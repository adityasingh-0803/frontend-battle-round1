import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import FrontendBattlePage from './FrontendBattlePage';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FrontendBattlePage />
  </React.StrictMode>
);
